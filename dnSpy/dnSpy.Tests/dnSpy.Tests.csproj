<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net472</TargetFramework> <!-- Assuming .NET Framework 4.7.2 for compatibility -->
    <IsPackable>false</IsPackable>
    <AssemblyName>dnSpy.dnSpy.Tests</AssemblyName>
    <RootNamespace>dnSpy.dnSpy.Tests</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.NET.Test.Sdk" Version="17.0.0" />
    <PackageReference Include="MSTest.TestAdapter" Version="2.2.8" />
    <PackageReference Include="MSTest.TestFramework" Version="2.2.8" />
  </ItemGroup>

  <ItemGroup>
    <!-- Reference the main dnSpy project -->
    <ProjectReference Include="..\dnSpy\dnSpy.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!-- This is needed if dnSpy.Contracts.App is in a different assembly and not directly referenced by dnSpy.csproj -->
    <!-- Add other references if AiCodeExplainerService depends on them and they aren't pulled transitively -->
  </ItemGroup>

</Project>
